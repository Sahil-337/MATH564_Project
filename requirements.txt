# Dependencies for the Linthurst regression analysis.
# Versions are pinned so the notebook runs exactly as written and all
# outputs are reproducible. Install with:  pip install -r requirements.txt

# --- Core analysis libraries (all imported in the notebook) ---
numpy==1.26.4
pandas==2.1.4
scipy==1.11.4
statsmodels==0.14.1
scikit-learn==1.3.2
matplotlib==3.8.2

# --- To open and run the notebook ---
jupyter==1.0.0
